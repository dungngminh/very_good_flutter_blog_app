abstract class HiveBox {
  static const bookmark = 'bookmark';
  static const userPrefs = 'prefs';
}

abstract class HiveBoxKey {
  static const isFirstTime = 'first_time';
  static const darkMode = 'dark_mode';
}
